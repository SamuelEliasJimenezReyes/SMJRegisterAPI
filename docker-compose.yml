version: "3.9"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8080}:8080"
    environment:

      - ASPNETCORE_URLS=http://+:${PORT:-8080}
      - DOTNET_ENVIRONMENT=Production
    restart: on-failure
